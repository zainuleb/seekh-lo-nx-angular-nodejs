import { CurrencyConvertPipe } from './currency-convert.pipe';

describe('CurrencyConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
